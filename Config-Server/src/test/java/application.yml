server:
  port: 5001

spring:
  cloud:
    config:
      server:
         git:
          uri: https://github.com/ankaiah504/configuration_properties.git
#          default-label: main
#          clone-on-start: true 
          force-pull: true
management:
  endpoints:
    web:
      exposure:
        include: '*'